# .env (used in dev; can also be read at build time in CI)
BACKEND_URL=http://localhost:5000         # used by Vite dev proxy only
VITE_BACKEND_URL=https://api.example.com  # only if the browser needs an absolute URL